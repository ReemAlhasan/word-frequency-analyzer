# word-frequency-analyzer
Java text processing application - analyzes word frequencies from text files with GUI for sorting, filtering, and searching. Includes text processor engine and Swing-based book reader interface.
